name: school_server
description: A server for the SSU project.

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  dart_jsonwebtoken: ^2.12.0
  shelf_multipart: ^2.0.1
  shelf_static: ^1.1.2
  bcrypt: ^1.1.3
  sqlite3: ^2.4.0
  path: ^1.9.0
  mailer: ^6.0.1
  dotenv: ^4.2.0

dev_dependencies:
  test: ^1.24.0
  mocktail: ^0.3.0
