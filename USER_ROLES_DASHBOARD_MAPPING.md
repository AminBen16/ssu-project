# Consolidated User Roles to Dashboard Mapping

This report consolidates how each `UserRole` is mapped to its dashboard implementation, detailing the dashboard entry points, related files, access logic, and implementation status based on the analysis of `user_roles.dart`, `user_data_provider.dart`, `staff_auth_service.dart`, `auth_wrapper.dart`, and the dashboard screen files.

## Summary of User Roles and Their Dashboard Mapping

| UserRole Name     | Dashboard Entry Point        | Related Files                                                                                                                                                                                                                                    | Access Logic & Notes                                                                                                                                                                                                                                                                                                                                                                                                                   | Implementation Status                                                                                                                                                                                                                                                                                                                                                                                                                          |
| :---------------- | :--------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pending`         | (No direct dashboard)        | `AuthWrapper`, `EmailVerificationPromptScreen`                                                                                                                                                                                                   | `AuthWrapper` checks `UserDataStatus.unauthenticated` or `UserDataStatus.authenticated` directly; `pending` is a role within `UserProfile` but not a direct routing condition for dashboards after authentication in `AuthWrapper`. Likely handled by `LoginScreen` or subsequent screens (e.g., `EmailVerificationPromptScreen`). | Partially Implemented (Defined and convertible, but no explicit routing to a dedicated dashboard or feature handling for this specific authenticated state shown in main routing flow.)                                                                                                                                                                                                                               |
| `chiefAdmin`      | `SystemAdminDashboardScreen` | `AuthWrapper`, `StaffAuthService`, `DashboardScreen`, `SystemAdminDashboardScreen` (via `lib/screens/system_admin_dashboard_screen.dart`), `CreateSchoolScreen` (accessed from `SystemAdminDashboardScreen`)                                    | `AuthWrapper`: `userProfile.role == UserRole.chiefAdmin && !userProfile.isFirstTimeSetupComplete` (routes to `AdminSetupScreen`); otherwise `DashboardScreen` which then dispatches to `SystemAdminDashboardScreen()`. `StaffAuthService.getDashboardRoute` returns `/system-admin-dashboard`. | Fully Implemented (Dedicated setup flow, unique dashboard routing to `SystemAdminDashboardScreen`, strong presence in access control logic, access to `CreateSchoolScreen`.)                                                                                                                                                                                                                                               |
| `headTeacher`     | `AdminDashboard`             | `StaffAuthService`, `DashboardScreen`, `AdminDashboard` (via `lib/widgets/dashboards/admin_dashboard.dart`)                                                                                                                                      | `DashboardScreen` dispatches to `AdminDashboard()` if `StaffAuthService().isAdminRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/admin-dashboard`. `UserDataProvider.isAdmin` also true.                                                                                                                                                       | Partially Implemented (Grouped under generic `AdminDashboard()`. Specific functionalities within this dashboard for `headTeacher` are inferred but not detailed in analyzed code. `getDashboardTitle` customizes its label.)                                                                                                                                                         |
| `director`        | `AdminDashboard`             | `StaffAuthService`, `DashboardScreen`, `AdminDashboard` (via `lib/widgets/dashboards/admin_dashboard.dart`)                                                                                                                                      | `DashboardScreen` dispatches to `AdminDashboard()` if `StaffAuthService().isAdminRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/admin-dashboard`. `UserDataProvider.isAdmin` also true.                                                                                                                                                       | Partially Implemented (Grouped under generic `AdminDashboard()`. Specific functionalities within this dashboard for `director` are inferred but not detailed in analyzed code. `getDashboardTitle` customizes its label.)                                                                                                                                                           |
| `deputyHeadTeacher` | `AdminDashboard`             | `StaffAuthService`, `DashboardScreen`, `AdminDashboard` (via `lib/widgets/dashboards/admin_dashboard.dart`)                                                                                                                                      | `DashboardScreen` dispatches to `AdminDashboard()` if `StaffAuthService().isAdminRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/admin-dashboard`. `UserDataProvider.isAdmin` also true.                                                                                                                                                       | Partially Implemented (Grouped under generic `AdminDashboard()`. Specific functionalities within this dashboard for `deputyHeadTeacher` are inferred but not detailed in analyzed code. `getDashboardTitle` customizes its label.)                                                                                                                                         |
| `systemAdmin`     | `AdminDashboard`             | `StaffAuthService`, `DashboardScreen`, `AdminDashboard` (via `lib/widgets/dashboards/admin_dashboard.dart`)                                                                                                                                      | `DashboardScreen` dispatches to `AdminDashboard()` if `StaffAuthService().isAdminRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/admin-dashboard`. `UserDataProvider.isAdmin` and `UserRole.enrollmentRoles.contains(role)` are also true.                                                                                                    | Partially Implemented (Grouped under generic `AdminDashboard()`. Explicitly has enrollment capabilities. Specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                      |
| `teacher`         | `TeacherDashboard`           | `StaffAuthService`, `DashboardScreen`, `TeacherDashboard` (via `lib/widgets/dashboards/teacher_dashboard.dart`)                                                                                                                                     | `DashboardScreen` dispatches to `TeacherDashboard()` if `StaffAuthService().isTeachingRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/teacher-dashboard`. `UserRole.isTeacher` is true.                                                                                                                                                             | Partially Implemented (Grouped under generic `TeacherDashboard()`. Specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                                                             |
| `student`         | `StudentDashboard`           | `UserDataProvider`, `StaffAuthService`, `DashboardScreen`, `StudentDashboard` (via `lib/widgets/dashboards/student_dashboard.dart`)                                                                                                                               | `UserDataProvider` triggers `StudentOfflineSyncService` on profile load. `DashboardScreen` dispatches to `StudentDashboard()` if `userRole == UserRole.student`. `StaffAuthService.getDashboardRoute` returns `/student-dashboard`.                                                                                                                                   | Fully Implemented (Dedicated data synchronization, unique dashboard routing to `StudentDashboard`, strong presence in core workflows like `UserDataProvider`.)                                                                                                                                                                                                           |
| `parent`          | `ParentDashboard`            | `StaffAuthService`, `DashboardScreen`, `ParentDashboard` (via `lib/widgets/dashboards/parent_dashboard.dart`)                                                                                                                                       | `DashboardScreen` dispatches to `ParentDashboard()` if `userRole == UserRole.parent`. `StaffAuthService.getDashboardRoute` returns `/parent-dashboard`.                                                                                                                                                                                                                 | Partially Implemented (Has dedicated dashboard routing to `ParentDashboard` and is a distinct role, but specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                |
| `nonTeachingStaff`| `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true (and not covered by other specific roles). `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                | Inferred/Generic (Acts as a catch-all for specific non-teaching roles, grouped under `StaffDashboard()`. Lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `classTeacher`    | `TeacherDashboard`           | `StaffAuthService`, `DashboardScreen`, `TeacherDashboard` (via `lib/widgets/dashboards/teacher_dashboard.dart`)                                                                                                                                     | `DashboardScreen` dispatches to `TeacherDashboard()` if `StaffAuthService().isTeachingRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/teacher-dashboard`. `UserRole.isTeacher` is true.                                                                                                                                                             | Partially Implemented (Grouped under generic `TeacherDashboard()`. Specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                                                             |
| `headOfDepartment`| `TeacherDashboard`           | `StaffAuthService`, `DashboardScreen`, `TeacherDashboard` (via `lib/widgets/dashboards/teacher_dashboard.dart`)                                                                                                                                     | `DashboardScreen` dispatches to `TeacherDashboard()` if `StaffAuthService().isTeachingRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/teacher-dashboard`. `UserRole.isTeacher` is true.                                                                                                                                                             | Partially Implemented (Grouped under generic `TeacherDashboard()`. Specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                                                             |
| `directorOfStudies`| `TeacherDashboard`           | `StaffAuthService`, `DashboardScreen`, `TeacherDashboard` (via `lib/widgets/dashboards/teacher_dashboard.dart`)                                                                                                                                     | `DashboardScreen` dispatches to `TeacherDashboard()` if `StaffAuthService().isTeachingRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/teacher-dashboard`. `UserRole.isTeacher` is true.                                                                                                                                                             | Partially Implemented (Grouped under generic `TeacherDashboard()`. Specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                                                             |
| `schoolAdmin`     | `AdminDashboard`             | `StaffAuthService`, `DashboardScreen`, `AdminDashboard` (via `lib/widgets/dashboards/admin_dashboard.dart`), `StudentEnrollmentScreen` (via FAB on `DashboardScreen`)                                                                      | `DashboardScreen` dispatches to `AdminDashboard()` if `StaffAuthService().isAdminRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/admin-dashboard`. `UserDataProvider.isAdmin` and `UserRole.enrollmentRoles.contains(role)` are also true. Has `StudentEnrollmentScreen` access via FAB.             | Partially Implemented (Grouped under generic `AdminDashboard()`. Explicitly has enrollment capabilities. Specific functionalities are inferred. `getDashboardTitle` customizes its label.)                                                                                                                                                                                      |
| `bursar`          | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`), `SchoolBudgetScreen` (via `lib/screens/school_budget_screen.dart`), `ExpenseReportsScreen` (via `lib/screens/expense_reports_screen.dart`), `FinancialReportsScreen` (via `lib/screens/financial_reports_screen.dart`) | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role. Role-specific features include 'School Budget', 'Expense Reports', and 'Financial Reports' with dedicated screens. | Partially Implemented (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label. Core financial features ('School Budget', 'Expense Reports', 'Financial Reports') have dedicated screens with frontend-safe stubs for future backend integration.)                                                                                                                                                                                            |
| `schoolSecretary` | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `librarian`       | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `labTechnician`   | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `computerLabAttendant` | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `schoolNurse`     | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `counselor`       | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `securityGuard`   | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `caretaker`       | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `cook`            | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `driver`          | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `storeKeeper`     | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `boardingMaster`  | `StaffDashboard`             | `StaffAuthService`, `DashboardScreen`, `StaffDashboard` (via `lib/widgets/dashboards/staff_dashboard.dart`)                                                                                                                                         | `DashboardScreen` dispatches to `StaffDashboard()` if `StaffAuthService().isStaffRole(userRole)` is true. `StaffAuthService.getDashboardRoute` returns `/staff-dashboard`. `UserRole.allNonTeachingRoles` includes this role.                                                                                                                                               | Inferred/Generic (Grouped under `StaffDashboard()`. `getDashboardTitle` customizes its label, but lacks dedicated individual implementation beyond generic staff features.)                                                                                                                                                                                            |
| `unknown`         | (No direct dashboard)        | `UserRole.fromString`                                                                                                                                                                                                                            | Fallback for roles that cannot be matched to a defined `UserRole` from a string.                                                                                                                                                                                                                                                                                             | Unused (As a primary assigned role), Generic fallback (Serves as a safe default for parsing, but not an actively assigned or routed role.)                                                                                                                                                                                                                               |
